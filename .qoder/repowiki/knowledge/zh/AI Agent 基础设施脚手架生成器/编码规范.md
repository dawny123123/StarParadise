- 采用“骨架+填充”模式，脚本创建带 `{{PARAM}}` 标记的确定性文件，代理仅负责替换参数化内容而不改变文件结构。
- 强制执行跨文件架构一致性，要求 `AGENTS.md`、`ARCHITECTURE.md` 与 `lint-deps` 脚本中的层级映射（Layer Map）保持严格同步。
- Linter 错误消息遵循 WHAT+WHY+HOW 格式，确保 AI Agent 能够理解违规原因并获取具体的修复建议。
- 设计文档生成遵循硬门禁规则，要求 `docs/design-docs/index.md` 必须在所有组件文档创建完成后编写，并确保索引与物理文件一一对应。