根目录 Makefile 提供统一入口：make setup-env 调用 harness 初始化环境，make start-server 启动后端，make build 构建 pc-admin 生产包，make lint-arch 执行依赖层级与质量检查，make verify 顺序运行 scripts/verify/*.sh 完成端到端验证，python3 scripts/validate.py . 触发完整验证流水线。