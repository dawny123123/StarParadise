通过 `npm run harness:setup`（即 `harness/scripts/setup-env.sh`）一次性安装 server / pc-admin / miniprogram 三方依赖；`npm run harness:start`（即 `harness/scripts/start-server.sh`）以后台方式启动 `star-park/server/src/index.js` 并轮询健康端点；`npm run harness:teardown` 清理环境。所有命令均基于 `environment.json` 中声明的 scripts 入口。