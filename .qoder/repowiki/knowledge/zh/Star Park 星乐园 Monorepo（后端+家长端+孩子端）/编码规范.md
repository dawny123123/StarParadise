- 新增业务模块需在 server/src/routes 下新增路由文件并在 app.js 中注册，前后端通过统一的 /api/* REST 路径约定通信。
- 数据库变更需同时维护 DDL 与 seed 数据，并通过 scripts/check-db-consistency.sh 校验一致性。
- 所有可重复的工程操作（构建、测试、文档生成、环境管理）均通过根 Makefile 暴露 target，禁止在子工程中散落命令。
- 业务规则（打卡进度、奖励目标达成、积分/余额变动）集中实现在 server 层，前端仅负责展示与调用。