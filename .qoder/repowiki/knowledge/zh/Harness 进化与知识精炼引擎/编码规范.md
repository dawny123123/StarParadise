- 所有辅助脚本均通过 `SKILL_DIR/scripts/` 路径动态调用，禁止在编排逻辑中内联实现复杂的数据解析或模式匹配。
- 进化操作遵循严格的分级处理策略：P0 级关键盲区自动应用，P1 级重要改进需用户确认，P2/P3 级仅记录待办。
- 涉及文件修改的操作（如 Skill 修补、记忆修剪）必须前置创建备份，并在验证失败时支持自动回滚。
- 知识提取与记忆合并步骤具有幂等性，每次运行均无条件触发以确保从最新 Episode 中捕获潜在教训。