- 每个可执行需求在 `exec-specs/` 下以独立目录承载，包含 `spec.md`（背景/目标/范围/约束/风险/技术方案/依赖图）作为单一事实来源。
- 已完成的需求归档到 `completed/<feature>/` 子目录，并补充 `tasks.md` 与 `acceptance.md` 两个配套文件，形成 spec→tasks→acceptance 的三段式交付物。
- 设计文档通过 mermaid 图表表达包依赖图、序列图和请求处理流，并在末尾用「来源」注释标注对应源码路径，保证文档与实现同步。