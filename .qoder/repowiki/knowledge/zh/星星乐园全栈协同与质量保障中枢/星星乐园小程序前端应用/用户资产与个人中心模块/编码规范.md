- 所有页面均实现“多儿童”上下文切换逻辑，通过顶部 Tab 栏选择 `activeChildId` 并触发数据重载。
- UI 组件高度复用卡片式设计模式，统一应用 `$radius-card` 圆角与 `$shadow-card` 阴影变量。
- API 调用遵循统一的 `request` 封装规范，在 `onMounted` 和 `onShow` 生命周期中执行数据初始化与刷新。
- 空状态处理标准化，当列表数据为空时统一展示 `.empty` 样式块及提示文本。