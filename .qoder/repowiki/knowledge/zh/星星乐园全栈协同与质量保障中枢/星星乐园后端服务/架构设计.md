- 采用 Express.js 构建轻量级 HTTP 服务，入口文件 `src/index.js` 负责路由注册与中间件配置。
- 数据层由 `src/database.js` 统一管理，使用 `better-sqlite3` 进行同步数据库操作，并在模块加载时自动执行建表与字段迁移。
- 业务逻辑分散在 `src/routes/` 下的多个路由模块中（如 `checkins.js`, `points.js`），直接依赖数据库实例处理 CRUD 及事务逻辑。
- 启动时通过 `src/seed.js` 初始化基础数据，确保服务可用性与数据一致性。