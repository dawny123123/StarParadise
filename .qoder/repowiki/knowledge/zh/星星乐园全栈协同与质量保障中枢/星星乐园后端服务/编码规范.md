- 所有路由模块均采用 Express Router 模式，并通过 `module.exports` 导出路由器实例以实现模块化挂载。
- 涉及多表状态变更的操作（如打卡并发放奖励、添加积分）均使用 `db.transaction()` 包裹以确保原子性。
- 数据库 schema 的演进通过在 `database.js` 中执行带异常捕获的 `ALTER TABLE` 语句实现，以兼容旧版数据库文件。