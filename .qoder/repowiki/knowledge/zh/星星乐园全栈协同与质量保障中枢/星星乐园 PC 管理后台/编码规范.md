- API 响应适配模式：在 API 层或组件请求逻辑中统一处理后端返回结构，兼容直接数组或 { data: [] } 格式（如 `Array.isArray(data) ? data : (data.data || [])`）。
- 字段命名转换规范：前端表单使用驼峰命名（camelCase），提交至后端时转换为下划线命名（snake_case），接收后端数据时再映射回驼峰格式供 UI 渲染。
- 多租户/多用户维度隔离：在任务、奖励等业务视图中，通过顶部 Tabs 组件切换当前操作的“孩子”ID，并利用 computed 属性对全量数据进行前端过滤展示。