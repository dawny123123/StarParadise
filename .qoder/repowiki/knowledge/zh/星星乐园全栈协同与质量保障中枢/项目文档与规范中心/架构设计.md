- **分层结构**：包含顶层核心规范（ARCHITECTURE, DEVELOPMENT, OPERATIONS）、子系统详细设计（design-docs/）、执行规格说明（exec-specs/）及 API 接口定义。
- **内容边界**：`ARCHITECTURE.md` 定义了前后端分层依赖规则；`api.md` 确立了 RESTful 交互契约；`exec-specs/` 记录了具体功能模块的迭代需求与技术实现路径。
- **关联机制**：通过 `scripts/lint-deps.py` 等工具将文档中的架构约束转化为可执行的代码检查逻辑，确保设计与实现的一致性。