根目录执行 `make setup-env` 初始化 harness 环境，`make start-server` 启动后端服务，`make api-doc` 生成 docs/api.md，`make verify` 运行 scripts/verify 下全部端到端场景；star-park/package.json 提供 install:all/start:server/start:pc/start:mini 快捷脚本。