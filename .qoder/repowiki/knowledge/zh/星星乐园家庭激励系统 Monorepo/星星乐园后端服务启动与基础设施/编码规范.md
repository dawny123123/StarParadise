- 数据库连接以单例形式导出，并在模块顶层统一执行建表与增量迁移，新增字段通过 try/catch 包裹 ALTER TABLE 实现幂等升级。
- 所有写操作使用 `db.prepare(...).run()` 配合占位符参数，避免 SQL 拼接。
- 多记录写入通过 `db.transaction(() => {...})` 包裹，保证种子数据与业务数据的原子性。
- 路由按业务域拆分到独立 router 模块，并在 `app.js` 中以 `/api/<domain>` 前缀统一挂载。