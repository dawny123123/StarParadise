使用根目录 `Makefile` 提供的统一命令集：`make build`（构建 pc-admin）、`make test`（运行测试并维持 ≥50% 行覆盖率）、`make lint-arch`（检查 L0–L5 层违规）、`make api-doc`（从源码生成 `docs/api.md`）、`make check-db`（验证 DDL 与 Entity 一致性）、`make verify`（端到端验证）。