`python3 scripts/validate.py . [--skip-verify]` 作为统一入口依次触发 `make build/lint-arch/test/verify`；`scripts/run-api-tests.sh` 依赖 `BASE_URL`（默认 `http://localhost:3001/api`）且要求服务已启动；`scripts/check-db-consistency.sh` 依赖 `star-park/server/data/star-park.db` 文件存在。