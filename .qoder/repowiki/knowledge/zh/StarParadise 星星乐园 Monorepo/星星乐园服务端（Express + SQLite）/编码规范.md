- 每个业务领域（children/tasks/checkins/rewards/stats/points/goals/todos）独立一个 routes/*.js 文件，并在 app.js 中以 /api/<domain> 前缀统一挂载
- 数据库初始化、表结构迁移与种子数据在应用启动阶段串行执行（seed → app.listen），确保首次访问前状态就绪
- 测试采用集成测试模式：tests/*.test.js 通过 supertest 直接调用同一 Express app，复用 database.js 的 SQLite 实例进行端到端验证