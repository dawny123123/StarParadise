- 页面按功能域拆分至 `src/pages/*/index.vue`，路由由 `src/pages.json` 统一管理而非代码内跳转
- API 请求通过 `src/api/index.js` 统一封装，页面直接调用该模块暴露的方法而不自行处理网络细节
- 可复用 UI 元素以 `.vue` 组件形式存放于 `src/components/`，由多个页面按需引入使用
- 全局样式与主题变量集中在 `src/uni.scss`，tabbar 图标统一放置于 `src/static/tabbar/`