- 所有 UI 组件均使用 Vue 3 <script setup> 语法糖定义逻辑，配合 defineProps 和 defineEmits 实现标准化通信。
- 样式实现强制引用 uni.scss 中的全局设计令牌（如 $primary, $radius-card），禁止在组件内硬编码颜色或尺寸值。
- API 调用统一通过 api/index.js 导出的封装对象进行，避免在组件中直接使用底层网络请求接口。