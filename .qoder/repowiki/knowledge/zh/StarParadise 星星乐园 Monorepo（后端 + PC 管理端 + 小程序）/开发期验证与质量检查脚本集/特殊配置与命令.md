`validate.py` 作为统一入口：`python3 scripts/validate.py .` 依次执行 `make build → make lint-arch → make test → [可选] make verify`；可通过 `--skip-verify` 跳过 E2E 验证。Bash 脚本中的参数化区域由 `# --- BEGIN PARAMETERIZED ---` / `# --- END PARAMETERIZED ---` 注释块包裹，便于外部模板替换。