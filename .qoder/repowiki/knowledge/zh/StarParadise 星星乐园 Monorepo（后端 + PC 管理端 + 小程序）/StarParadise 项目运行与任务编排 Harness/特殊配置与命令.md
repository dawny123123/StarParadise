`make setup` → 执行 `harness/scripts/setup-env.sh` 安装三端 node_modules；`make start` → 执行 `harness/scripts/start-server.sh` 启动 Express 服务；`make teardown` → 执行清理脚本；`make verify` → 运行 `harness/verify/` 下的所有自包含 shell 脚本（exit 0 表示成功）。